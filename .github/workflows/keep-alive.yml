name: Keep Render Backend Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    
    steps:
      - name: Ping Render Backend
        run: |
          curl -f https://vibein-backend.onrender.com/api/health || echo "Ping failed"
        
      - name: Log Status
        run: echo "Pinged backend at $(date)"